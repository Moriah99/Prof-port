
  # Refine portfolio layout

  This is a code bundle for Refine portfolio layout. The original project is available at https://www.figma.com/design/j0j2k8ZTVEdnMalmGSxris/Refine-portfolio-layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  